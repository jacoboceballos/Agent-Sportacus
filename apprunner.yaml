version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  runtime-version: 3.11
  command: cd backend && python -m uvicorn simple_ai_server:app --host 0.0.0.0 --port 8000
  network:
    port: 8000
    env: PORT